{
    "method": "GET",
    "url": "{{endpoint}}/siem/v1/events?limit=10",
    "headers": {
        "Accept": "*/*",
        "x-api-key": "{{client_id}}",
        "Authorization": "{{secret}}"
    },
    "body": "",
    "expect": {
        "response": 200,
        "body": "ANY"
    }
}
